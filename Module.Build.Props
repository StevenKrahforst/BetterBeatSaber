<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

    <PropertyGroup>
        <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
        <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
        <OutputType>Library</OutputType>
        <TargetFrameworkVersion>v4.8</TargetFrameworkVersion>
        <FileAlignment>512</FileAlignment>
        <LangVersion>latest</LangVersion>
        <OutputPath>bin\</OutputPath>
        <RootNamespace>BetterBeatSaber.$(ModuleName)</RootNamespace>
        <AssemblyName>BetterBeatSaber.$(ModuleName)</AssemblyName>
        <AppDesignerFolder>Properties</AppDesignerFolder>
    </PropertyGroup>

    <Target Name="CopyToModulesDirectory" AfterTargets="Build">
        <Copy SourceFiles="$(OutputPath)$(AssemblyName).dll" DestinationFiles="$(BetterBeatSaberModulesDirectory)\$(ModuleId).dll" />
    </Target>

    <ItemGroup>
        <ProjectReference Include="..\BetterBeatSaber\BetterBeatSaber.csproj">
            <Project>{f4d05faa-f8b3-45c0-9f23-07f36a2fd944}</Project>
            <Name>BetterBeatSaber</Name>
            <Private>False</Private>
        </ProjectReference>
    </ItemGroup>

    <ItemGroup>
        <EmbeddedResource Include="module.json" />
    </ItemGroup>

</Project>